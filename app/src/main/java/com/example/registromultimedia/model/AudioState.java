package com.example.registromultimedia.model;

/**
 * Enum for audio recording and playback states.
 */
public enum AudioState {
    IDLE,
    RECORDING,
    RECORDED,
    PLAYING,
    PAUSED,
    ERROR
}
